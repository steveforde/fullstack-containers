name: E2E Tests

on:
  push:
    branches: [ main, master ]

jobs:
  e2e-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          clean: true

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Debug Test File Content
        run: |
          echo "--- CURRENT COMMIT ---"
          git log -1 --oneline
          echo "--- CHECKING PATH TO TEST FILE ---"
          find . -name "todo-app.spec.js"
          echo "--- LINES 15-25 OF TEST FILE ---"
          if [ -f todo-tests/tests/todo-app.spec.js ]; then
            sed -n '15,25p' todo-tests/tests/todo-app.spec.js
          else
            echo "File not found at standard path, searching..."
          fi

      - name: Build todo-backend image
        working-directory: ./todo-app/todo-backend
        run: docker build --no-cache -t todo-backend -f Dockerfile .

      - name: Build todo-frontend image
        working-directory: ./todo-app/todo-frontend
        run: docker build --no-cache -t todo-front -f Dockerfile .

      - name: Start todo-app with Docker Compose
        working-directory: ./todo-app
        run: docker compose up -d --build --force-recreate

      - name: Wait for services to be ready
        run: |
          echo "Waiting for frontend on port 8080..."
          timeout 120 bash -c 'until curl -f http://localhost:8080 > /dev/null 2>&1; do sleep 2; done'
          
          echo "Waiting for API (/api/todos) to respond..."
          timeout 120 bash -c 'until curl -f http://localhost:8080/api/todos > /dev/null 2>&1; do sleep 2; done'
          
          echo "Waiting for statistics endpoint..."
          timeout 60 bash -c 'until curl -f http://localhost:8080/api/statistics > /dev/null 2>&1; do sleep 2; done'
          
          echo "All services are ready!"

      - name: Check running containers
        run: docker ps

      - name: Install dependencies
        working-directory: ./todo-tests
        run: npm ci

      - name: Install Playwright browsers
        working-directory: ./todo-tests
        run: npx playwright install --with-deps chromium

      - name: Run Playwright tests
        working-directory: ./todo-tests
        run: npm run test:e2e

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: todo-tests/playwright-report/
          retention-days: 7

      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: todo-tests/test-results/
          retention-days: 7

      - name: Show container logs on failure
        if: failure()
        working-directory: ./todo-app
        run: |
          echo "=== Container logs ==="
          docker compose logs

      - name: Clean up Docker containers
        if: always()
        working-directory: ./todo-app
        run: docker compose down -v
